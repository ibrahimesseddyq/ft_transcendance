apiVersion: apps/v1
kind: Deployment
metadata:
  name: quiz-service
  namespace: hirefy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quiz-service
  template:
    metadata:
      labels:
        app: quiz-service
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "quiz-service"
        vault.hashicorp.com/agent-inject-secret-database: "secret/data/quiz-service/database"
        vault.hashicorp.com/agent-inject-template-database: |
          {{- with secret "secret/data/quiz-service/database" -}}
          export DATABASE_URL="{{ .Data.data.DATABASE_URL }}"
          {{- end }}
        vault.hashicorp.com/agent-inject-secret-jwt: "secret/data/quiz-service/jwt"
        vault.hashicorp.com/agent-inject-template-jwt: |
          {{- with secret "secret/data/quiz-service/jwt" -}}
          export ACCESS_TOKEN_SECRET="{{ .Data.data.ACCESS_TOKEN_SECRET }}"
          export REFRESH_TOKEN_SECRET="{{ .Data.data.REFRESH_TOKEN_SECRET }}"
          {{- end }}
    spec:
      serviceAccountName: app-service-account
      containers:
        - name: quiz-service
          image: quiz-service:dev
          envFrom:
            - configMapRef:
                 name: quiz-service-config
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3001
              protocol: TCP
          command:
            - /bin/sh
            - -c
            - |
              # Source Vault secrets
              . /vault/secrets/database
              . /vault/secrets/jwt
              # Start the application
              npm run dev
---
apiVersion: v1
kind: Service
metadata:
  name: quiz-service
  namespace: hirefy
spec:
  selector:
    app: quiz-service
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: quiz-service-config
  namespace: hirefy
data:
  PORT: "3001"
  NODE_ENV: "production"
  APP_NAME: "quiz-service"
  HOST: "quiz-service"
  BCRYPT_ROUNDS: "10"
  CALLBACK_URL: "http://13.38.15.15/api/main/auth/google/callback"
  ACCESS_TOKEN_EXPIRY: "1d"
  REFRESH_TOKEN_EXPIRY: "7d"
  FRONTEND_URL: "http://13.38.15.15"
  BACKEND_URL: "http://13.38.15.15/api/main"