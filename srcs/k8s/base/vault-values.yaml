ui:
  enabled: true

server:
  dev:
    enabled: true
    devRootToken: "root"
  enabled: true
  replicas: 1

  dataStorage:
    enabled: true
    size: 1Gi
    mountPath: /vault/data 
  
  standalone:
    enabled: true
  
  extraEnvironmentVars:
    VAULT: "http://127.0.0.1:8200"