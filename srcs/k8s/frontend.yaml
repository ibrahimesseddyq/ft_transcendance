apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: hirefy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: frontend:dev
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: front-service-config
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          env:
            - name: VITE_API_URL 
              value: "http://gateway:8081"
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: hirefy
spec:
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: front-service-config
  namespace: hirefy
data:
  VITE_SERVICE_URL: "http://13.36.189.126"
  VITE_MAIN_API_URL: "/api/main"
  VITE_QUIZ_API_URL: "/api/quiz"
  VITE_RAG_API_URL: "/api/rag"
  VITE_AI_API_URL: "/api/ai"