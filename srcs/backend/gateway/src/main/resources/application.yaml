server:
  port: 8081
  http2:
    enabled: true

spring:
  application:
    name: api-gateway
  main:
    web-application-type: reactive

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty: DEBUG
    org.springframework.web: DEBUG